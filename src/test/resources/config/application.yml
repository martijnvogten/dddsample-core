spring:
    jpa:
        show-sql: true
    dataSource:
        url: jdbc:hsqldb:mem:dddsample_test
    main:
        allow-bean-definition-overriding: true
server:
    error:
        include-message: always
logging:
    level:
        jdbc: trace
